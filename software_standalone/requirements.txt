# Core runtime dependencies
numpy>=1.24
PyYAML>=6.0

# Optional extras -----------------------------------------------------------
# Uncomment the lines below if you plan to interact with real hardware or use
# plotting helpers. They are already declared as extras in pyproject.toml, but
# providing them here helps for quick `pip install -r requirements.txt`.

# smbus2>=0.4            # PCA9685 hardware backend (Linux/Raspberry Pi)
# matplotlib>=3.8        # Stick-figure plotting / diagnostics
